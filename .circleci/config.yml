# .circleci/config.yml (CircleCI 2.1)
version: 2.1

executors:
  docker-dind:
    machine:
      image: ubuntu-2404:current
      docker_layer_caching: false
    environment:
      DOCKER_BUILDKIT: 1
      COMPOSE_DOCKER_CLI_BUILD: 1

jobs:
  build_and_push:
    executor: docker-dind
    steps:
      - checkout

      - run:
          name: Log in to Docker Hub
          command: |
            echo "$DOCKERHUB_PAT" | docker login -u "$DOCKERHUB_USER" --password-stdin

      - run:
          name: Check for CRLF line endings
          command: |
            if grep -rlI $'\r' ./; then
              echo "ERROR: CRLF line endings detected! Convert all scripts to LF before building."
              exit 1
            else
              echo "✅ No CRLF detected. All files are LF."
            fi

      - run:
          name: Build & push image
          command: |
            # Use Git tag as version
            TAG="${CIRCLE_TAG}"
            if [ -z "$TAG" ]; then
              echo "Error: This job should only run on Git tags."
              exit 1
            fi

            IMG="docker.io/$DOCKERHUB_USER/comfyui-qwen-template"

            echo "Building and pushing ${IMG}:${TAG} ..."
            docker build --progress=plain -t "${IMG}:${TAG}" .
            docker push "${IMG}:${TAG}"

            # Also update latest
            echo "Tagging and pushing ${IMG}:latest ..."
            docker tag "${IMG}:${TAG}" "${IMG}:latest"
            docker push "${IMG}:latest"

workflows:
  version: 2
  build_on_tag:
    jobs:
      - build_and_push:
          context: docker-hub
          filters:
            tags:
              only: /^v[0-9]+(\.[0-9]+)*$/   # Only Git tags like v1, v1.2, v1.2.3
            branches:
              ignore: /.*/                   # Ignore all branch pushes